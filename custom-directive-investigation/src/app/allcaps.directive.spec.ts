import { AllcapsDirective } from './allcaps.directive';

describe('AllcapsDirective', () => {
  it('should create an instance', () => {
    const directive = new AllcapsDirective();
    expect(directive).toBeTruthy();
  });
});
